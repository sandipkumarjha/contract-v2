/**
 * The Markets section (list, stock pages, buy/sell) still runs on mock trading and is
 * locked while it is rebuilt. Every /markets route renders the upgrade notice instead.
 * Set to false to reopen it.
 */
export const MARKETS_LOCKED = true;
